/// Controls where the loading indicator is placed relative to the button label.
enum LoaderPosition {
  /// Shows only the loading indicator, hiding the label entirely.
  center,

  /// Places the loading indicator to the left of the label.
  left,

  /// Places the loading indicator to the right of the label.
  right,

  /// Overlays the loading indicator on top of the button content.
  overlay,
}
